# Repository map

This repository is the GitHub profile README, not an executable application. Its runtime boundary is GitHub rendering the root README.md.

~~~mermaid
flowchart LR
    Profile[genesisgzdev README] --> TDS[Threat Detection Suite]
    Profile --> Mega[Project MegaTicketing]
    Profile --> Nexus[Nexus Intelligence]
    Profile --> CalcX[CalcX Advanced]
    Profile --> Aegis[Aegis11]
~~~

The project table is a curated index. Its status text is intentionally narrower than the individual repositories: it distinguishes automated checks from Windows, WDK, database and network runtime validation that cannot be inferred from a profile page.
